# Cloudflare Worker Development Variables
# Copy this file to .dev.vars and fill in your values
# .dev.vars is used by `wrangler dev` for local development

# Container Authentication (for production deployment)
# Set this when deploying containers in production mode
WORKER_API_KEY=your-secure-random-api-key-here

# Example: Generate a secure key with:
# openssl rand -base64 32
# or
# node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
